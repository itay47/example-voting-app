apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-vote.fullname" . }}-test-connection"
  labels:
{{ include "my-vote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "my-vote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
