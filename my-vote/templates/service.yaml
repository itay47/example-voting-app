apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-vote.fullname" . }}
  labels:
    {{- include "my-vote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-vote.selectorLabels" . | nindent 4 }}
